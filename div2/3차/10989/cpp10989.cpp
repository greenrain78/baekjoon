#include <iostream>
#include <algorithm>

using namespace std;

int main()
{
    int count;
    //input
    cin >> count;
    int num[count];
    for (size_t i = 0; i < count; i++)
    {
        cin >> num[i];
    }
    //sort
    // sort(num, num + count);
    // for (size_t i = 0; i < count; i++)
    // {
    // cout << num[i] << endl;
    // }
}